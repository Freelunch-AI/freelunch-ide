{
  "name": "@freelunch/ide-core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types and constants for the FreeLunch IDE frontend",
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "clean": "tsc --build tsconfig.build.json --clean",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
