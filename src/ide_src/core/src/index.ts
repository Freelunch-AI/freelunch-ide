/**
 * Shared constants and types for the FreeLunch IDE frontend.
 *
 * The IDE workbench itself is built on Eclipse Theia and is scoped to roadmap
 * section 7.2; this package exists so the TypeScript build, lint, and test
 * toolchain has a real target before that work starts.
 */

export * from './product';
