interface:
  display_name: "Pull Request"
  short_description: "Open a linked draft pull request"
  default_prompt: "Use $pr to prepare and open a draft pull request with evidence."
policy:
  allow_implicit_invocation: false
