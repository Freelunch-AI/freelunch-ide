interface:
  display_name: "Plan"
  short_description: "Plan an issue without changing the repository"
  default_prompt: "Use $plan to analyze the GitHub issue and produce an implementation-ready plan."
policy:
  allow_implicit_invocation: false
