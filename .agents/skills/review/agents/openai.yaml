interface:
  display_name: "Review"
  short_description: "Review the current issue branch independently"
  default_prompt: "Use $review to review the current issue branch for correctness, security, and drift."
policy:
  allow_implicit_invocation: false
