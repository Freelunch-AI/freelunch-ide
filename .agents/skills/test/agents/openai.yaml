interface:
  display_name: "Test"
  short_description: "Test the current issue implementation"
  default_prompt: "Use $test to add missing tests and run the applicable checks."
policy:
  allow_implicit_invocation: false
